command:
  - "python"
  - "app.py"

env:
  - name: APP_NAME
    value: "Customer Billing Intelligence"

  - name: SERVING_ENDPOINT_NAME
    valueFrom: "serving-endpoint"

  # Lakebase connection (Track C) — auto-injected when Lakebase resource is added
  # - name: DB_CONNECTION_STRING
  #   valueFrom: "database"
